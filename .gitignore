# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.production

# IDE/Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build/Distribution
dist/
build/

# Database backups
*.backup.json
*.old.json

# Runtime JSON stores — may hold real intake/contact data (incl. minors).
# Child-safety: these must NEVER be committed. Real data lives only in the
# private Supabase project, not in git. (Pre-existing tracked db/*.json files
# predate this rule and need a separate scrub — see the onboarding intake work.)
db/onboarding_intakes.json

# Local dev auth fallback — real store is Supabase `users`; never commit hashes
db/users.json
